import { generateMetadata } from "@/helpers";

export const metadata = generateMetadata({
  title: "Edit Accept Terms",
  description: "Edit InReDD' Accept Terms",
});

export default function Layout({ children }) {
  return children;
}

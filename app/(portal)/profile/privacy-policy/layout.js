import { generateMetadata } from "@/helpers";

export const metadata = generateMetadata({
  title: "Edit Privacy Policy",
  description: "Edit InReDD' Privacy Policy",
});

export default function Layout({ children }) {
  return children;
}
